{
  "name": "app",
  "private": "true",
  "scripts": {
    "build:css": "echo 'CSS is now handled by Rails asset pipeline'",
    "watch:css": "echo 'CSS is now handled by Rails asset pipeline'"
  },
  "browserslist": [
    "defaults"
  ],
  "dependencies": {}
}
